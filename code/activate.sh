# run 'source activate.sh' to activate the conda environment
# this is like a shortcut instead of writing the whole command below
# type "source act" and press tab to autocomplete the command
conda activate conda/